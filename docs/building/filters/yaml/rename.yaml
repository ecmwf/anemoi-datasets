input:
  pipe:
  - source:
      # mars, grib, netcdf, etc.
      # source attributes here
      # ...

  - rename:
      param:
        # Map old `param` names to new ones
        temperature_2m: 2t
        temperature_850hPa: t_850
        # ...
